spring:
 application:
  name: api-gateway

 security:
  oauth2:
   resourceserver:
    jwt:
     jwk-set-uri: http://localhost:9000/oauth2/jwks

 cloud:
  loadbalancer:
   enabled: true

  gateway:
   server:
    webflux:
     routes:
      - id: msv-categorias
        uri: lb://msv-categorias
        predicates:
         - Path=/api/categorias/**
        filters:
         - StripPrefix=2

      - id: msv-proveedores
        uri: lb://msv-proveedores
        predicates:
         - Path=/api/proveedores/**
        filters:
         - StripPrefix=2

      - id: msv-productos
        uri: lb://msv-productos
        predicates:
         - Path=/api/productos/**
        filters:
         - StripPrefix=2

server:
 port: 8090

eureka:
 client:
  service-url:
   defaultZone: http://localhost:8761/eureka